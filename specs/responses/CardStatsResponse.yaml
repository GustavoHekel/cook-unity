title: Cards response
type: object
required:
  - data
properties:
  data:
    type: object
    properties:
      card:
        $ref: '../schemas/Entities.yaml#/components/schemas/Card'
      weaknesses:
        type: array
        items:
          $ref: '../schemas/Entities.yaml#/components/schemas/Card'
      resistances:
        type: array
        items:
          $ref: '../schemas/Entities.yaml#/components/schemas/Card'