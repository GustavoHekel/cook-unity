title: Cards response
type: object
required:
  - data
properties:
  data:
    type: object
    properties:
      cards:
        type: array
        items:
          $ref: '../schemas/Entities.yaml#/components/schemas/Card'
      types:
        type: array
        items:
          $ref: '../schemas/Entities.yaml#/components/schemas/Type'
      rarities:
        type: array
        items:
          $ref: '../schemas/Entities.yaml#/components/schemas/Rarity'